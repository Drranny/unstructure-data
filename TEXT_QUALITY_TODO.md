# 텍스트 품질 검사 - 미구현 항목

## 🔮 미구현 고급 기능

### 1. 맥락 단절 검사 (Context Break Detection)

**현재 상태:** ✅ 구현 완료

**설명:**
- 문장 간 주제 변화가 급격한 경우를 감지
- 예: "오늘은 날씨가 좋다. 나는 파스타." (주제가 급격히 바뀜)

**구현 방안:**
```python
def check_context_break(sentences):
    """
    Sentence Transformer로 문장 간 유사도 계산
    유사도가 매우 낮으면 맥락 단절 가능
    """
    model = get_model()  # 기존 SentenceTransformer 모델 활용
    embeddings = model.encode(sentences)
    
    # 연속된 문장 간 유사도 계산
    for i in range(len(embeddings) - 1):
        similarity = cosine_similarity(
            embeddings[i].reshape(1, -1),
            embeddings[i+1].reshape(1, -1)
        )[0][0]
        
        # 유사도가 매우 낮으면 (예: < 0.3) 맥락 단절 가능
        if similarity < 0.3:
            return True  # 맥락 단절 감지
    
    return False
```

**필요한 라이브러리:**
- `sentence-transformers` (이미 설치됨)
- `scikit-learn` (cosine_similarity)

**우선순위:** ✅ 완료

**구현 내용:**
- `check_context_break()` 함수 추가
- SentenceTransformer를 활용한 연속 문장 간 유사도 계산
- 유사도가 0.3 미만이면 맥락 단절로 판단
- `check_text_accuracy()` 함수에 통합

---

### 2. OCR 오류 감지 (OCR Error Detection)

**현재 상태:** ❌ 미구현

**설명:**
- OCR로 인한 잘못 인식된 텍스트 감지
- 예: "샤오뤼시과곰나사" - 비정상적인 한글 조합

**구현 방안:**
```python
def check_ocr_error(text):
    """
    1. 한글 단어 사전과 비교
    2. 자모 조합 유효성 검사
    3. 비정상적인 음절 조합 감지
    """
    # 방법 1: 한글 단어 사전 활용
    # - 한국어 형태소 분석기 (konlpy) 사용
    # - 사전에 없는 비정상적 조합 감지
    
    # 방법 2: 자모 조합 패턴 검사
    # - 한글 자모 조합 규칙 위반 감지
    # - 예: 받침이 없는 자모 조합 등
    
    pass
```

**필요한 라이브러리:**
- `konlpy` (한국어 형태소 분석)
- 또는 한글 사전 데이터

**우선순위:** 낮음

---

### 3. 고급 문법 검사 (Advanced Grammar Check)

**현재 상태:** ❌ 미구현 (기본 패턴만 구현됨)

**설명:**
- 현재는 패턴 기반 문법 오류만 감지
- 고급 문법 오류는 외부 API나 모델 필요
- 예: "데이터 품질는 중요하다" → "품질은" (조사 오류)

**구현 방안:**
```python
def check_advanced_grammar(text):
    """
    1. 외부 문법 검사 API 연동
       - 한국어 문법 검사 API (예: 네이버 맞춤법 검사 API 확장)
       - 또는 언어 모델 활용
    
    2. 언어 모델 기반 검사
       - GPT 등 언어 모델로 문법 교정
       - 원문과 교정문 비교
    """
    pass
```

**필요한 리소스:**
- 외부 문법 검사 API
- 또는 대규모 언어 모델 (GPT 등)

**우선순위:** 낮음

---

## 📊 구현 우선순위

1. ✅ **맥락 단절 검사** (완료)
   - 기존 SentenceTransformer 모델 활용
   - 추가 라이브러리 설치 필요 없음

2. **OCR 오류 감지** (낮은 우선순위)
   - 형태소 분석기 설치 필요
   - 구현 난이도: 높음

3. **고급 문법 검사** (낮은 우선순위)
   - 외부 API 또는 대규모 모델 필요
   - 구현 난이도: 매우 높음

---

## ✅ 이미 구현된 항목들

- ✅ 문법 오류 패턴 (기본)
- ✅ 형식 불일치 검사
- ✅ 불완전한 문장 패턴
- ✅ 부적절한 표현 검사
- ✅ 영어/한국어 오탈자 검사
- ✅ 데이터 내 중복 검사
- ✅ 반복 패턴 강화

