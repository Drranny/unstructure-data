# 기술 스택 및 프레임워크 정리

본 문서는 AI 학습용 비정형데이터 품질진단 프로그램에서 사용된 모든 프레임워크와 기술을 정리한 문서입니다.

---

## 📋 목차

1. [웹 프레임워크](#웹-프레임워크)
2. [이미지 처리 기술](#이미지-처리-기술)
3. [텍스트 처리 기술](#텍스트-처리-기술)
4. [딥러닝 프레임워크](#딥러닝-프레임워크)
5. [데이터셋 처리](#데이터셋-처리)
6. [수치 연산 및 유틸리티](#수치-연산-및-유틸리티)
7. [보고서 생성](#보고서-생성)
8. [데이터 처리](#데이터-처리)
9. [requirements.txt 전체 목록](#requirementstxt-전체-목록)

---

## 🎨 웹 프레임워크

### Streamlit (≥1.28.0)
- **용도**: 웹 대시보드 및 UI 구축
- **사용 위치**: `app.py` (메인 애플리케이션)
- **주요 기능**:
  - 파일 업로드 인터페이스
  - 데이터 시각화 (차트, 테이블)
  - 탭 기반 UI 구성
  - 세션 상태 관리
  - PDF 다운로드 기능
- **특징**: Python 기반의 빠른 웹 앱 개발 프레임워크

---

## 🖼️ 이미지 처리 기술

### OpenCV (opencv-python ≥4.8.0)
- **용도**: 이미지 품질 분석 (선명도, 노이즈 측정)
- **사용 위치**: `src/image_quality.py`
- **주요 기능**:
  - Laplacian Variance를 통한 선명도 계산
  - Gaussian Blur를 이용한 노이즈 측정
  - 이미지 색상 공간 변환 (RGB ↔ BGR ↔ Grayscale)
- **핵심 함수**:
  - `cv2.Laplacian()`: 선명도 계산
  - `cv2.GaussianBlur()`: 노이즈 측정
  - `cv2.cvtColor()`: 색상 공간 변환

### Pillow (PIL) (pillow ≥10.0.0)
- **용도**: 이미지 파일 로드 및 처리
- **사용 위치**: 전체 프로젝트
- **주요 기능**:
  - 이미지 파일 열기/저장
  - 이미지 형식 변환
  - 이미지 크기 조정
- **사용 예시**:
  ```python
  from PIL import Image
  img = Image.open("image.jpg")
  ```

### ImageHash (imagehash ≥4.3.1)
- **용도**: 이미지 중복도 검사
- **사용 위치**: `src/image_quality.py`, `src/dataset_analyzer.py`
- **주요 기능**:
  - Average Hash를 통한 이미지 유사도 계산
  - 중복 이미지 감지
- **핵심 함수**:
  - `imagehash.average_hash()`: 이미지 해시 생성
  - 해시 간 차이 계산으로 중복도 측정

---

## 📝 텍스트 처리 기술

### Sentence Transformers (sentence-transformers ≥2.2.0)
- **용도**: 문장 임베딩 생성 및 유사도 분석
- **사용 위치**: `src/text_quality.py`
- **주요 기능**:
  - 텍스트 중복도 계산 (문장 간 유사도)
  - 다국어 모델 지원 (`paraphrase-multilingual-MiniLM-L12-v2`)
  - 코사인 유사도 계산
- **사용 모델**:
  - 기본: `paraphrase-multilingual-MiniLM-L12-v2` (다국어 지원)
  - Fallback: `paraphrase-MiniLM-L6-v2` (영어 전용)
- **핵심 함수**:
  - `SentenceTransformer.encode()`: 문장 임베딩 생성
  - `util.pytorch_cos_sim()`: 코사인 유사도 계산

### Transformers (transformers ≥4.30.0)
- **용도**: Hugging Face 모델 로드 및 사용
- **사용 위치**: `src/text_quality.py` (간접 사용)
- **주요 기능**:
  - 사전 훈련된 모델 로드
  - Sentence Transformers의 백엔드

### PyEnchant (pyenchant ≥3.2.0)
- **용도**: 영어 맞춤법 검사
- **사용 위치**: `src/text_quality.py`
- **주요 기능**:
  - 영어 단어 오탈자 자동 감지
  - 사전 기반 맞춤법 검사
- **주의사항**: 시스템 패키지 `enchant` 필요
  - Ubuntu/Debian: `sudo apt-get install enchant`
  - 또는: `sudo apt-get install libenchant-2-dev python3-enchant`
- **Fallback**: 패턴 기반 검사 (라이브러리 없을 경우)

### Py-Hanspell (py-hanspell ≥1.1)
- **용도**: 한국어 맞춤법 검사
- **사용 위치**: `src/text_quality.py`
- **주요 기능**:
  - 한국어 맞춤법 오류 자동 감지
  - API 기반 맞춤법 검사
- **Fallback**: 패턴 기반 검사 (라이브러리 없을 경우)

---

## 🧠 딥러닝 프레임워크

### PyTorch (torch ≥2.0.0)
- **용도**: 딥러닝 모델 실행 및 텐서 연산
- **사용 위치**: `src/text_quality.py` (Sentence Transformers 백엔드)
- **주요 기능**:
  - 텐서 연산 (코사인 유사도 계산)
  - GPU 지원 (선택적)
- **특징**: Sentence Transformers가 내부적으로 PyTorch 사용

### Torchvision (torchvision ≥0.15.0)
- **용도**: CIFAR-10 데이터셋 로드
- **사용 위치**: `src/dataset_analyzer.py`
- **주요 기능**:
  - CIFAR-10 데이터셋 자동 다운로드 및 로드
  - 이미지 변환 처리
- **핵심 함수**:
  - `datasets.CIFAR10()`: CIFAR-10 데이터셋 로드

---

## 📦 데이터셋 처리

### Hugging Face Datasets (datasets ≥2.14.0)
- **용도**: Hugging Face 데이터셋 다운로드 및 로드
- **사용 위치**: `src/dataset_analyzer.py`
- **주요 기능**:
  - 다양한 데이터셋 자동 다운로드
  - Streaming 모드 지원 (메모리 효율적)
  - Split 선택 (train/test/validation)
  - 퍼센티지 기반 샘플링
- **핵심 함수**:
  - `load_dataset()`: 데이터셋 로드
  - `load_dataset_builder()`: 데이터셋 메타데이터 조회
  - `get_dataset_config_names()`: 사용 가능한 config 목록

### Hugging Face Hub (huggingface_hub ≥0.20.0)
- **용도**: Hugging Face 데이터셋 검색 및 메타데이터 조회
- **사용 위치**: `src/dataset_finder.py`
- **주요 기능**:
  - 데이터셋 검색 (검색어, 태스크 필터)
  - 인기 데이터셋 목록 조회
  - 데이터셋 메타데이터 조회 (splits, configs)
- **핵심 함수**:
  - `HfApi().list_datasets()`: 데이터셋 검색
  - `HfApi().dataset_info()`: 데이터셋 정보 조회

---

## 🔢 수치 연산 및 유틸리티

### NumPy (numpy ≥1.24.0)
- **용도**: 수치 연산 및 배열 처리
- **사용 위치**: 전체 프로젝트
- **주요 기능**:
  - 이미지 배열 처리
  - 통계 계산 (평균, 표준편차, 중앙값)
  - 배열 연산
- **사용 예시**:
  - 이미지 품질 점수 계산
  - 데이터셋 통계 계산

### Scikit-learn (scikit-learn ≥1.3.0)
- **용도**: 머신러닝 유틸리티 함수
- **사용 위치**: 전체 프로젝트 (간접 사용)
- **주요 기능**:
  - 유틸리티 함수 제공
  - 통계 계산 지원

---

## 📄 보고서 생성

### ReportLab (reportlab ≥4.0.0)
- **용도**: PDF 보고서 생성
- **사용 위치**: `src/utils.py`
- **주요 기능**:
  - 텍스트 품질 분석 보고서 PDF 생성
  - 이미지 품질 분석 보고서 PDF 생성
  - 데이터셋 배치 분석 보고서 PDF 생성
  - 한글 폰트 지원 (NotoSansKR)
- **핵심 함수**:
  - `SimpleDocTemplate()`: PDF 문서 생성
  - `Table()`: 테이블 생성
  - `Paragraph()`: 텍스트 단락 생성
- **폰트**: NotoSansKR (Regular, Bold) 사용

---

## 📊 데이터 처리

### Pandas (pandas ≥2.0.0)
- **용도**: 데이터프레임 처리 및 표시
- **사용 위치**: `app.py`, `src/quality_evaluator.py`
- **주요 기능**:
  - 개별 점수 데이터프레임 생성
  - 정렬 및 필터링
  - 라벨링 기반 평가 데이터 처리
- **참고**: Streamlit이 내부적으로 pandas 사용

### ROUGE Score (rouge-score ≥0.1.2)
- **용도**: 텍스트 생성 작업의 유효성 평가
- **사용 위치**: `src/quality_evaluator.py`
- **주요 기능**:
  - ROUGE-1, ROUGE-2, ROUGE-L 점수 계산
  - 텍스트 생성 품질 평가
- **핵심 함수**: `rouge_score.rouge_scorer.RougeScorer`

### NLTK (nltk ≥3.8)
- **용도**: BLEU 점수 계산
- **사용 위치**: `src/quality_evaluator.py`
- **주요 기능**:
  - BLEU 점수 계산 (n-gram 정밀도)
  - 텍스트 생성 품질 평가
- **핵심 함수**: `nltk.translate.bleu_score`

### Jiwer (jiwer ≥3.0)
- **용도**: Character Error Rate (CER) 계산
- **사용 위치**: `src/quality_evaluator.py`
- **주요 기능**:
  - 문자 오류율 계산
  - 텍스트 생성 품질 평가
- **핵심 함수**: `jiwer.cer`

---

## 📋 requirements.txt 전체 목록

### 웹 프레임워크
```
streamlit>=1.28.0          # 웹 대시보드 및 UI
```

### 이미지 처리
```
opencv-python>=4.8.0       # 이미지 품질 분석 (선명도, 노이즈)
pillow>=10.0.0             # 이미지 파일 로드 및 처리
imagehash>=4.3.1           # 이미지 중복도 검사
```

### 텍스트 처리
```
sentence-transformers>=2.2.0  # 문장 임베딩 및 유사도 분석
transformers>=4.30.0          # Hugging Face 모델 로드
pyenchant>=3.2.0              # 영어 맞춤법 검사
py-hanspell>=1.1              # 한국어 맞춤법 검사
```

### 딥러닝
```
torch>=2.0.0                # PyTorch (딥러닝 프레임워크)
torchvision>=0.15.0         # CIFAR-10 데이터셋 로드
```

### 데이터셋
```
datasets>=2.14.0            # Hugging Face 데이터셋 로드
huggingface_hub>=0.20.0      # Hugging Face 데이터셋 검색
```

### 수치 연산
```
numpy>=1.24.0                # 수치 연산 및 배열 처리
scikit-learn>=1.3.0          # 머신러닝 유틸리티
```

### 보고서 생성
```
reportlab>=4.0.0            # PDF 보고서 생성
```

### 라벨링 기반 평가 (선택적)
```
rouge-score>=0.1.2          # ROUGE 점수 계산
nltk>=3.8                   # BLEU 점수 계산
jiwer>=3.0                  # CER (Character Error Rate) 계산
pandas>=2.0.0               # 데이터프레임 처리
```

---

## 🔧 기술 스택 아키텍처

```
┌─────────────────────────────────────────────────────────┐
│                    Streamlit (UI Layer)                   │
│  - 파일 업로드, 결과 표시, 차트 시각화                    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│              품질 분석 모듈 (Analysis Layer)              │
│  ┌──────────────────┐  ┌──────────────────┐              │
│  │ text_quality.py │  │ image_quality.py │              │
│  │ - Sentence      │  │ - OpenCV         │              │
│  │   Transformers  │  │ - ImageHash     │              │
│  │ - PyEnchant     │  │ - Pillow        │              │
│  │ - Py-Hanspell   │  │                 │              │
│  └──────────────────┘  └──────────────────┘              │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│            데이터셋 처리 모듈 (Data Layer)                │
│  ┌──────────────────┐  ┌──────────────────┐            │
│  │ dataset_analyzer │  │ dataset_finder   │            │
│  │ - Torchvision    │  │ - Hugging Face   │            │
│  │ - Datasets       │  │   Hub            │            │
│  │ - Hugging Face   │  │                  │            │
│  └──────────────────┘  └──────────────────┘            │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│              보고서 생성 모듈 (Report Layer)              │
│  ┌──────────────────┐                                    │
│  │ utils.py         │                                    │
│  │ - ReportLab      │                                    │
│  │ - PDF 생성       │                                    │
│  └──────────────────┘                                    │
└─────────────────────────────────────────────────────────┘
```


## 🔍 기술 선택 이유

### Streamlit
- **선택 이유**: Python 기반으로 빠른 프로토타이핑 가능, 데이터 시각화 내장
- **대안**: Flask, Django, FastAPI (더 복잡하지만 더 많은 제어 가능)

### Sentence Transformers
- **선택 이유**: 다국어 지원, 문장 유사도 계산에 최적화
- **대안**: BERT 직접 사용 (더 복잡), TF-IDF (정확도 낮음)

### OpenCV
- **선택 이유**: 이미지 처리의 표준 라이브러리, 성능 우수
- **대안**: scikit-image (기능 제한적), PIL만 사용 (기능 부족)

### Hugging Face Datasets
- **선택 이유**: 다양한 데이터셋 지원, Streaming 모드로 메모리 효율적
- **대안**: 직접 다운로드 (더 복잡), 다른 데이터셋 플랫폼 (제한적)

---

## 📝 추가 참고사항

### 시스템 의존성
- **pyenchant**: 시스템 패키지 `enchant` 필요
  ```bash
  sudo apt-get install enchant
  # 또는
  sudo apt-get install libenchant-2-dev python3-enchant
  ```

### 선택적 의존성
- **py-hanspell**: API 기반이므로 인터넷 연결 필요
- **pyenchant**: 없으면 패턴 기반 검사로 fallback

### 성능 최적화
- **Streaming 모드**: Hugging Face Datasets에서 대용량 데이터셋 처리 시 사용
- **샘플링**: 대용량 데이터셋 분석 시 일부만 샘플링하여 성능 향상

---

## 🔄 버전 호환성

- **Python**: 3.11 이상 권장
- **PyTorch**: 2.0.0 이상 (CUDA 지원 선택적)
- **Streamlit**: 1.28.0 이상

---

## 📚 참고 문서

- [Streamlit 공식 문서](https://docs.streamlit.io)
- [Sentence Transformers 문서](https://www.sbert.net)
- [Hugging Face Datasets 문서](https://huggingface.co/docs/datasets)
- [OpenCV 문서](https://docs.opencv.org)
- [ReportLab 문서](https://www.reportlab.com/docs)


